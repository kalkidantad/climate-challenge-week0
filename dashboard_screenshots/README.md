# Dashboard screenshots (optional)

After running `streamlit run app/main.py` with your local cleaned CSVs, capture a screenshot of the app and save it here (e.g. `dashboard_main.png`) for the Week 0 PDF report. This folder is safe to commit; **do not** commit raw `data/` CSVs.
